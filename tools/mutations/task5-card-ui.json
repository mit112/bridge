[
  {
    "name": "drop the handoff term from the card sort key",
    "file": "src/bridge/cards.py",
    "old": "    if card.handoff:\n        rank = RANK_HANDOFF\n    elif card.live is not None:",
    "new": "    if card.live is not None:",
    "tests": [
      "tests/test_cards.py::test_a_queued_handoff_sorts_above_a_dirty_and_stale_project"
    ]
  },
  {
    "name": "mark the prompt safe in the template (render it as live markup)",
    "file": "src/bridge/templates/_launch.html",
    "old": "data-prompt-handoff=\"{{ handoff.id }}\">{{ handoff.next_prompt }}</textarea>",
    "new": "data-prompt-handoff=\"{{ handoff.id }}\">{{ handoff.next_prompt | safe }}</textarea>",
    "tests": [
      "tests/test_api.py::test_a_queued_prompt_is_html_escaped_on_the_card"
    ]
  },
  {
    "name": "render the handoff block even with no handoff",
    "file": "src/bridge/templates/_launch.html",
    "old": "{% macro handoff_block(card, handoff, totals, collapse_prompt=False, show_span_line=False) %}\n  {% if handoff %}\n    {% set hid = \"handoff-\" ~ handoff.id %}",
    "new": "{% macro handoff_block(card, handoff, totals, collapse_prompt=False, show_span_line=False) %}\n  {% if True %}\n    {% set hid = \"handoff-\" ~ handoff.id %}",
    "tests": [
      "tests/test_api.py::test_a_card_with_no_handoff_shows_no_empty_affordance",
      "tests/test_launch_template.py::test_handoff_block_renders_nothing_for_a_falsy_handoff"
    ]
  },
  {
    "name": "weaken the work-soft surface so work-accent foreground drops below AA",
    "file": "src/bridge/static/app.css",
    "old": "  --p-work-soft: #f0e0d5;",
    "new": "  --p-work-soft: #a23c17;",
    "tests": [
      "tests/test_contrast.py"
    ],
    "expect_count": 2
  },
  {
    "name": "remove the live region from the copy confirmation",
    "file": "src/bridge/templates/_launch.html",
    "old": "        <span class=\"handoff__status\" role=\"status\" aria-live=\"polite\"\n              data-copy-status=\"{{ hid }}\"></span>",
    "new": "        <span class=\"handoff__status\" data-copy-status=\"{{ hid }}\"></span>",
    "tests": [
      "tests/test_api.py::test_the_card_shows_the_handoff_and_a_labelled_copy_affordance"
    ]
  },
  {
    "name": "stop passing handoff history to the project workspace",
    "file": "src/bridge/workspace.py",
    "old": "        handoffs = store.handoffs(project_id, limit=50)",
    "new": "        handoffs = []",
    "tests": [
      "tests/test_api.py::test_the_project_page_lists_past_handoffs_with_their_status"
    ]
  }
]
